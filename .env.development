# Empty in dev: fetch uses relative paths → vite.config.js proxy forwards
# /api and /media to the backend (default http://localhost:51820, override
# via VITE_API_TARGET when starting `npm run dev`). This avoids CORS.
VITE_API_BASE_URL=
